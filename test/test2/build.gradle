wrapper {
    gradleVersion = '7.4.2'
}

allprojects {
    group = pGroup
    version = pVersion
}

subprojects {
    apply plugin: 'eclipse'
    apply plugin: 'maven-publish'

    repositories {
        mavenCentral()
    }
}
